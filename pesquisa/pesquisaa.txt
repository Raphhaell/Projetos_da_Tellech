/* Estilos para a barra de pesquisa */
.search-container {
    position: fixed; /* Defina a posição como fixa */
    top: 20px; /* Ajuste a distância a partir do topo da janela */
    right: 20px; /* Ajuste a distância a partir da direita da janela */
    background-color: #006494;
    padding: 10px;
    border-radius: 8px;
    text-align: right;
    z-index: 1000; /* Garante que a barra esteja acima de outros elementos */
}

#search-input {
    width: 350px;
    padding: 15px;
    font-size: 18px;
    border: none;
    outline: none;
    transition: width 0.3s ease;
    border-radius: 8px;
}

#search-input:hover {
    width: 400px;
}

#search-button {
    padding: 15px 30px;
    font-size: 18px;
    background-color: transparent;
    color: #fff;
    border: 2px solid #006494;
    border-radius: 8px;
    cursor: pointer;
    transition: background-color 0.3s ease, transform 0.3s ease;
}

#search-button:hover {
    background-color: #005580;
    transform: scale(1.1);
}